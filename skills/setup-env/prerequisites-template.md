# Prerequisites

<!--
Starting point. Fill the sections in for your project and delete the comments.
The setup-env skill reads this file as its source of truth: the tool list,
the platform commands, the verification block and the pitfalls all come from
here instead of being inferred from manifests.
-->

What has to be installed and configured on the machine before starting. macOS and Windows have their own sections; everything else is shared.

You do not have to work through this by hand — ask your coding agent to check your environment and it will walk the same list, install what it can, tell you what only you can do, and verify the result.

## 1. Accounts

<!-- Everything that needs a sign-up, with why this project needs it.
     If a service costs money to run continuously, say so here in plain words:
     it affects the decision to start at all. -->

## 2. What to install and why

<!-- The "why" column matters most — without it people install everything.
     The "required" column saves time for anyone who only needs the minimum. -->

| Tool | Why this project needs it | Minimum version | Required |
|---|---|---|---|
|  |  |  |  |

## 3. macOS

<!-- Package manager first, then the install commands.
     List separately anything that still has to be launched after installing. -->

## 4. Windows

<!-- If the project uses Docker or a Unix shell, open with the WSL2 decision:
     it determines every command that follows, and the human makes it. -->

## 5. Shared configuration

<!-- Git identity, line endings on Windows, environment variables. -->

## 6. Verification

<!-- A block that can be run as a whole. For each tool, a command that answers
     "is it working" rather than "is a file on disk". A tool with its own daemon
     is verified by reaching the daemon. -->

```bash

```

## 7. Common pitfalls

<!-- Only what has actually broken for real people. Hypothetical problems take
     up space here and lower trust in the rest of the list. -->
