# Adopted rules

- REUSE-1: action buttons use the shared button() factory in ui/button.js.
- COLOR-1: action button backgrounds use --action from ui/tokens.css, with no screen-local override.
- LAYOUT-1: action groups can use .stack with wrapping and shared spacing.
- VIEW-1: verify idle Account controls at widths 390 and 1280.

Typography for support text has not been decided. No other action states are in this fixture scope.
