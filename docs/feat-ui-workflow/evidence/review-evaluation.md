# Independent forward evaluation: Account review

Used fixture-installed sdlc-ui-review and linked contract/report guide with raw Account files and adopted revision-1 kit. No external expected answers consulted. No setup ceremony or implementation performed.

Observed behavior:

- Two violations: Save local color override (COLOR-1); Cancel hand-built duplicate (REUSE-1 / BTN-1).
- Save rendered magenta; Cancel and actual showcase rendered blue at widths 390 and 1280. Screenshots and computed styles confirm this.
- Cancel's visual match did not conceal its source reuse violation.
- Support-text italics correctly classified as an explicitly undecided kit gap, not a violation.
- Ordinary `.stack` composition accepted; no new catalog entry demanded. Rendered controls fit with 16px gap and no horizontal overflow; wrapping configured in shared source and computed styles.
- Original-file SHA-256 manifest pins source/kit/skills. All 19 original files unchanged. Only docs/ui-reviews report/evidence added within fixture.
- Initial rendering failed due sandbox bind restriction plus unavailable prescribed port. Escalated retry found 8769 occupied by unrelated Okta process, left untouched. With parent authorization, used existing Python server on 127.0.0.1:8876 without project changes. CUA rendered both Account and actual showcase at 390×844 and 1280×800. Temporary viewport override reset.
- Main mode final verdict: nonconforming, with complete source/rendered coverage for adopted idle action-control scope. Initial incomplete report and browser failure evidence preserved.
- Separate browser-unavailable mode ran source-only and remains explicitly incomplete despite confirmed source violations; it does not reuse rendered evidence from later recovery.

Artifacts under review/docs/ui-reviews/account:

- browser-attempt/ui-review.md — final source/rendered review
- browser-attempt/ui-review-before-runtime-recovery.md — preserved blocked-pass report
- browser-attempt/evidence/ — four screenshots, before/after original hashes, runtime/browser observations
- browser-unavailable/ui-review.md — constrained-host review
- write-audit.json — write boundary evidence

No app/kit fixes, dependency installs, baseline updates or acceptance-state changes. Kit ready label was not treated as current source-bound verification. Support typography remains undecided outside adopted action controls; other states are explicitly outside fixture scope. Alternate-port runtime was used solely for review and stopped afterward.
