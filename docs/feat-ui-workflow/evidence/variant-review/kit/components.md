# Components

BTN-1: button(label), import from ui/button.js; one default variant, idle state. Example: showcase.html. No other variants adopted.
Group composition: .stack from ui/tokens.css can combine approved controls; no separate reusable component needed.
